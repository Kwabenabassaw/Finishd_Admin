import 'package:flutter/material.dart';
class MLScreen extends StatelessWidget { const MLScreen({super.key}); @override Widget build(BuildContext context) => const Center(child: Text('ML Screen')); }
