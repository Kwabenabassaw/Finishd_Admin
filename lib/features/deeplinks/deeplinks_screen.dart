import 'package:flutter/material.dart';
class DeepLinksScreen extends StatelessWidget { const DeepLinksScreen({super.key}); @override Widget build(BuildContext context) => const Center(child: Text('Deep Links Screen')); }
